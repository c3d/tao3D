Message to show
Unicode UTF-8 encoding

Set color to white
Local change of attributes (attribute, texture)
Load the earth surface texture
Rotate the Earth around vertical axis
Sphere centered on origin, radius 250

Another local change of attributes
Load the background "space" texture
Rotate the background
Put the space "inside out" (we see the back)
Very large sphere around the center

Draw in a 900x300 pixels texture
Draw in white (we'll color it below)
Select font
... and size
Move to left of texture
Draw text

Rotate text sphere
Grey color, used for the shadow
Draw the text shadow (sphere just larger than earth)
Yellow color, used for main text
Draw the main text (sphere just larger than shadow)
