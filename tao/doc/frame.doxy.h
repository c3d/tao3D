/**
 * @addtogroup WindowsAndFrame Windows and frames
 * @ingroup TaoBuiltins
 * C'est quoi une Frame ??? Comment les windows, frames et pages s'agencent-ils ?
 *
 * @{
 */
/**
 * @fn window_height ()
 * @brief Return the height of window
 *
 *  Return the height, as areal, of window in which we display.
 * @returns Return the height of window in which we display.
 */
window_height ();

/**
 * @fn window_width ()
 * @brief Return the width of the window.
 *
 *  Return the width, as areal, of the window in which we display.
 * @returns Return the width of the window in which we display
 */
window_width ();

/**
 * @fn frame_depth ()
 * @brief Return the depth of the current layout frame
 *
 *  Return the depth of the current layout frame as a real.
 * @returns Return the depth of the current layout frame
 */
frame_depth ();

/**
 * @fn frame_height ()
 * @brief Return the height of the current layout frame
 *
 *  Return the height of the current layout frame as a real.
 * @returns Return the height of the current layout frame
 */
frame_height ();

/**
 * @fn frame_width ()
 * @brief Return the width of the current layout frame
 *
 *  Return the width of the current layout frame as a real.
 * @returns Return the width of the current layout frame
 */
frame_width ();

/** @} */
